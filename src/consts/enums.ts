export enum Priority {
    low = 1,
    medium = 2,
    high = 3
}

export enum Status {
    todo = "To-do",
    inProgress = "In Progress",
    complete = "Complete"
}

export enum TaskFilter {
    dueDate = "dueDate",
    priority = "priority",
}

export enum TaskSort {
    asc = "ASC",
    desc = "DESC"
}